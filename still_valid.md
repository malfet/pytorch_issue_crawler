# still_valid.md

Audit trail for issues that were **verified and deliberately left open** — the
counterpart to `deduplication.md` (which records *close* actions). An entry here
means: "I reproduced / investigated this and it is a genuine, live issue (or
otherwise should not be closed)." Append one row per decision; never overwrite.

Before re-verifying an old issue, check here first so you don't redo work already
done. If an issue listed here later gets closed, move/annotate it in
`deduplication.md` at that point.

| Date (UTC) | Issue — status | How verified | Reasoning / minimal repro |
|---|---|---|---|
| 2026-08-10 | #100334 (dsantiago) — **still reproduces** | Reconstructed the reporter's `ConvNet`, bisected on torch 2.13.0 (Homebrew py3.13 venv, MPS), M5 Pro | `module: mps` + `module: correctness (silent)`: "MPS inference all same value". Original repro wasn't self-contained (needed `net.pt`/datasets), so rebuilt from the posted `ConvNet`. Confirmed still broken on 2.13.0 and **minimized to a single op**: `nn.Conv1d` on MPS with input sequence length **exactly 32768 (2^15)** returns identical output for every batch element (batch dimension collapses); CPU is correct. Length-specific — 32767/32769 and other powers of two (16384/65536) are fine. Deterministic across runs. Posted minimal repro as a comment. |
| 2026-08-10 | #96602 (Birch-san) — **kept open** (do not close) | Ran both repro snippets on torch 2.13.0 (Homebrew py3.13 venv), M5 Pro / macOS 26.5.2, 64 GB | `module: mps`: `softmax` on a large `[10,12416,12416]` tensor returns NaN on MPS (NaN first appears at `diffs = x - maxes`), fp16 & fp32, on the 2.1 nightly. Original repro is **self-contained** and fits in RAM (~2.9 GB). On 2.13.0 it does **not** reproduce on my M5 Pro — no NaN in either dtype, output correct (~1/12416, rows sum to 1); matches maintainer @jhavukainen's earlier "fixed on 2.5.1" note. **NOT closed:** active 2026 comments report it still reproduces on **M5 Max** hardware (possibly a different FMA-precision mechanism, cf. #121558), most recent 2026-08-08 on a 2.13.0 nightly; a maintainer is actively investigating M5-specific cases. My M5 **Pro** / release-2.13.0 non-repro is a useful data point but not grounds to close. |
| 2026-08-10 | #88893 (htdai) — **improved, not fixed** | Built minimal double-backward repro on torch 2.13.0, M5 Pro / macOS 26.5.2 | `module: mps`: 2nd-order grad (Hessian) through `nn.MaxPool2d` on MPS raised `derivative for aten::mps_max_pool2d_backward is not implemented`. On 2.13.0 that hard error is gone, but a **real CPU-vs-MPS gap remains**: the reporter's pattern (double-backward through default `MaxPool2d`, `return_indices=False`) still works on CPU (`OK`) yet fails on MPS with a graceful `not infinitely differentiable … set return_indices=True`. Root cause (confirmed in source `derivatives.yaml`/`Pooling.cpp`): CPU `max_pool2d` is `CompositeImplicitAutograd` lowering to the double-differentiable `max_pool2d_with_indices`; MPS uses a standalone `mps_max_pool2d` whose backward has an explicit `error_for_max_pool2d_double_backward` stub. The `return_indices=True` path *does* double-backward on MPS (norm 0). Improved but not resolved → NOT closed. |
| 2026-08-11 | #44611 (mruberry) — **still reproduces** (NOT x86-only) | Ran repro on torch 2.13.0, M5 Pro / macOS 26.5.2 (arm64) | `module: macos`: complex `tan`/`tanh` at a pole. `torch.tan(-1.5707963267948966+0j)` → `(-inf+nanj)` and `torch.tanh(-1.5707963267948966j)` → `(nan-infj)`, vs NumPy's finite `-1.633e16`. **Deliberately excluded from the Intel-Mac won't-fix batch** — and rightly so: still reproduces on Apple Silicon, so it's a **macOS libm vs glibc** difference (Apple's complex `std::tan`/`tanh` overflows at the pole; NumPy uses its own complex-hyperbolic impl), not x86-specific. Left open. |
