# Deduplication log

Audit trail of duplicate-issue decisions. Newest entries at the bottom. See
`CLAUDE.md` for the recording policy and `dedupe_candidates.py` for how
candidates are generated.

| Date (UTC) | Decision | Action | Reasoning |
|------------|----------|--------|-----------|
| 2026-07-15 | #127009 → duplicate of #126947 | Closed on GitHub (`not planned`), commented "Duplicate of #126947" | Same author (`hyperkai`), filed the same day (~18h apart). Both report `dsplit()` with `indices_or_sections=` not working while the positional form works — #127009 is a reworded restatement of #126947. Cosine similarity 0.89; shared terms: `indices_or_sections`, `dsplit`, `torch.dsplit`, `sections`. |
| 2026-07-16 | PR #180891 → duplicate of PR #190014 (kept #190014) | Closed #180891 on GitHub, commented pointing to #190014 | Duplicate-effort PRs (different authors) for the same `rocm_smi`→`amd_smi` migration; #190014 is an explicit rebased sequel of #180891. Both CLA `SUCCESS`. Older #180891's **last commit** was 2026-04-20 (stale ~3 months) vs. #190014's 2026-07-15 and `MERGEABLE` — so under the PR rule the older/stale one is closed in favor of the newer active one. Note: #180891's `updatedAt` (2026-07-13) was misleading cc/bot comment noise; staleness was judged by last commit date. |
| 2026-07-17 | #107543 and #107556 (dmc1778) — closed **both**, not one-as-dup | Added `module: nn` label to both; closed both `not planned` with an "expected behavior" comment | Same case (surfaced as a near-duplicate pair, cosine 0.73): both call `AdaptiveAvgPool2d(~1.25e12)`, requesting a ~1.25e12² output whose `numel` overflows int64 (`numel: integer multiplication overflow`). Not an operator bug — nonsensical output size can't be allocated. Per maintainer decision, closed both as expected behavior rather than deduping one into the other. |
| 2026-07-17 | #91557 → duplicate of #91556 (dmc1778) | Closed #91557 `not planned` via `close_duplicate.py`; added `module: nn` (+ existing `module: crash`) | Identical reproducer to #91556 (`AdaptiveMaxPool3d([125091515651]*3)`). #91557 segfaulted on torch 1.12/1.13; the crash was later fixed by adding a checked error (`Storage size calculation overflowed`), which is what #91556 already shows — so same case, closed as duplicate. |
| 2026-07-17 | #91556 (dmc1778) — closed as expected behavior | Added `module: nn`; closed `not planned` with "expected behavior" comment | Requests an `AdaptiveMaxPool3d` output of 125091515651³ elements; storage size overflows int64 and is correctly guarded (`Storage size calculation overflowed`). Nonsensical output size, not a bug. #91557 (the segfault variant) is a duplicate of this. |
| 2026-07-17 | Correction: re-mark #91557 and #127009 with real `DUPLICATE` reason | GraphQL `closeIssue(stateReason:DUPLICATE, duplicateIssueId:…)` | They had been closed as `not_planned` (all `gh issue close --reason` exposes), which doesn't show the "marked as duplicate of #X" relationship. Re-closed via GraphQL so `state_reason=duplicate`. `close_duplicate.py` now does this automatically for issues. PRs (e.g. #180891) have no duplicate state_reason, so they stay closed-with-comment. Expected-behavior closes (#91556/#107543/#107556) correctly remain `not_planned`. |
| 2026-07-17 | #136568 → duplicate of #136452 (hyperkai) | Closed #136568 as `DUPLICATE` via `close_duplicate.py`; added `module: complex` to #136452 | Both report `linalg` norms returning a real `float` tensor for complex input while ignoring `dtype=torch.complex64` (torch 2.3.0). `linalg.norm` on a 1-D input dispatches to `linalg.vector_norm`, so #136452's vector case *is* #136568 — kept the older/general #136452. `matrix_norm` counterpart #136621 left open (distinct 2-D function). |
| 2026-07-18 | #136452 — repro test, **kept open** | Ran the reproducer on torch 2.8.0 (throwaway venv) | Still reproduces: `linalg.norm(complex, dtype=torch.complex64)` returns `tensor(8.6023)` as `float32`, silently ignoring `dtype` — identical to the 2.3.0 report. Not closing as fixed. (Nuance for maintainer: a complex vector's norm is real, so float output is arguably WAI; the only gripe is `dtype` being ignored rather than erroring.) |
| 2026-07-18 | #136452 — resolved as docs (by maintainer) | Now `closed/completed`, `module: docs` added, resolved via a docs update | Follow-up to the repro test above: the real fix was documentation (a complex vector's norm is floating-point by definition). Maintainer added `module: docs` and closed it `completed` after the doc update landed. DB re-synced. |
| 2026-07-18 | #91553 → duplicate of #91552 (dmc1778) | Closed #91553 as `DUPLICATE` via helper; added `module: nn` + `module: crash` | 2-D analog of #91556/#91557. Identical reproducer to #91552 (`AdaptiveMaxPool2d([125091515651]*2)`); #91553 segfaulted on 1.12, now a checked error. Verified on torch 2.8.0: raises `Storage size calculation overflowed`, no segfault. |
| 2026-07-18 | #91552 (dmc1778) — closed as expected behavior | Added `module: nn`; closed `not planned` | Requests an `AdaptiveMaxPool2d` output of 125091515651² elements; storage size overflows int64 and is correctly guarded (verified torch 2.8.0). Nonsensical output size, not a bug. #91553 is the segfault duplicate of this. |
| 2026-07-18 | #136186 → duplicate of #80574 (different authors) | Closed #136186 as `DUPLICATE` via helper | Same missing-kernel feature gap: bfloat16 support for `bmm_sparse_cuda` (sparse×dense bmm on CUDA). #80574 (lostmsu, 2022) is the older standing request; #136186 (leonardodalinky, 2024) hit `RuntimeError: "bmm_sparse_cuda" not implemented for 'BFloat16'`. CUDA-only path — not repro-tested (no GPU available); deduped as feature request, not asserted fixed. |
| 2026-07-18 | #116250, #116252, #116253 → duplicates of #116251 (yonghyeon1064) | Closed all three as `DUPLICATE` via helper | flip-family heap-buffer-overflow cluster (all filed 2023-12-21). **Confirmed in source** (`aten/src/ATen/native/TensorTransformations.cpp`): `rot90`/`fliplr`/`flipud` are implemented as `self.flip(...)`, and `flip` dispatches to `flip_stub` — so all four share the one flip kernel and the underlying fix is common. Kept #116251 (base `flip` op) as canonical. |
| 2026-07-19 | #153315 → duplicate of #153224 (zhouxiaoyaozzz) | Closed #153315 as `DUPLICATE`; added `module: correctness (silent)` to #153224 | Byte-for-byte identical reproducer and description (JIT/script vs eager discrepancy in `F.instance_norm(multiplied, x, x)`), filed one day apart. Verified still reproducing on torch 2.8.0 (`allclose=False`, max abs diff ~0.37), so #153224 kept open. Requested label was `module: correctness`; the actual repo label is `module: correctness (silent)`. |
| 2026-07-19 | #35553 → duplicate of #19708; #19708 closed obsolete | Closed #35553 as `DUPLICATE`; closed #19708 `not planned` (caffe2 removed) | Aliases of the same broken caffe2 `DEPTHWISE3x3.Conv` / depthwise3x3_conv test (#19708 by xkszltl 2019, #35553 by malfet 2020). caffe2 has been removed from PyTorch, so the test no longer exists — #35553 marked duplicate of the older #19708, and #19708 closed as obsolete. |
| 2026-07-19 | #140796, #140892, #140701, #141802 (usmonali4) — closed as expected behavior | Closed all four `not planned` with float64 references in each comment | "CPU vs GPU inconsistency" template family (`xlog1py`/`xlogy`/`polygamma`/`logcumsumexp`). Verified against float64 ground truth: all differences are at the **ULP level of the low-precision dtype** (bf16 for three, float32 for polygamma), i.e. expected rounding — the report tolerances (`atol=1e-2` / `1e-6`) are tighter than the dtype's precision. Which backend is closer to truth *varies* (GPU for xlog1py/xlogy, CPU for polygamma/logcumsumexp), so neither is wrong. Not deduped (distinct functions). `logcumsumexp` GPU side not re-run (not implemented on MPS); used reported CUDA value. |
| 2026-07-20 | #43604 → duplicate of #38090 (kept #38090 open) | Closed #43604 as `DUPLICATE`; left #38090 open per maintainer | Different authors, but identical `detectron2` `demo.py` traceback → `ImportError: libtorch_cpu.so` (#43604 even reused #38090's `/home/ty/` path). Root cause is a version mismatch (detectron2 `_C` built against a different PyTorch; 1.5.0 split `libtorch.so`, see #29235) — not a PyTorch bug, already explained in-thread. Only closed the newer duplicate; original #38090 kept open. |
| 2026-07-20 | #188423 → duplicate of #188422 (ydshieh) | Closed #188423 as `DUPLICATE`; added `module: inductor` + `module: sdpa` to #188422 and commented that sapiens2 is also affected | Same report filed the same minute (2026-06-29 14:36): identical setup and the same `test_sdpa_can_compile_dynamic` regression (torch 2.12 → 2.13 RC, `torch.compile(dynamic=True)`). #188422 listed segformer/efficientloftr, #188423 listed sapiens2 — folded the sapiens2 case into #188422. Needs CUDA + 2.13 RC, so not repro-tested. |
| 2026-07-20 | #188230 → duplicate of #188225 (jasminetrail) | Closed #188230 as `DUPLICATE` via helper | Same root cause: Inductor `index_propagation` applies `maximum`/`minimum` to a non-orderable sympy value → `ValueError: ... is not comparable` (`index_propagation.py`). #188225 = `nan` operand (confirmed still reproducing on torch 2.8.0 at `index_propagation.py:170`); #188230 = `bool` operand (did *not* repro on 2.8.0 — newer/path-specific). Deduped since the shared fix (guarding min/max against non-orderable operands) covers both — per maintainer, the fixing PR is the same. |
| 2026-07-20 | #182652 → duplicate of #182649 (ALinrunrun) | Closed #182652 as `DUPLICATE`; ported `module: viewing and reshaping` label to #182649 | Both crash `torch.compile`/Inductor while tracing an `onnx2torch`-converted model containing a `Reshape`, with the **identical** error `InternalTorchDynamoError: TypeError: torch.Size() takes an iterable of 'int' (item 0 is 'FakeTensor')`. Root cause: the Reshape's int64 shape tensor is a FakeTensor passed into `torch.Size()`. The two repros differ only in preceding ops (Conv-Reshape vs Add-Sub-Reshape). Not repro-tested locally (needs onnx/onnxruntime/onnx2torch), but error strings are identical. |
| 2026-07-22 | #143655 → duplicate of #98208 (cross-author) | Closed #143655 as `DUPLICATE` via helper | Same missing feature reported by two independent users ~20 months apart (EN #98208 by PerloDaniele 2023; ZH #143655 by tyth66 2024, title 反射填充的反向传播没有确定性实现 = "reflection pad backward has no deterministic impl"). Identical `reflection_pad2d_backward_cuda does not have a deterministic implementation` warning — only cosmetic diffs (`Context.cpp` line 82→91 across torch 1.13→2.5). Kept older/better-labelled #98208 (already `actionable`). |
| 2026-07-23 | #73181 → duplicate of #73180 (neochristou) | Closed #73181 as `DUPLICATE`; added `module: crash` to #73180 | Both sobol-engine ops (`_sobol_engine_initialize_state_` / `_sobol_engine_ff_`) hard-crash on an oversized `dimension`=1250999896764 (≫ MAXDIM ~21201) — no `dimension` validation → OOB native access. **Repro-verified on torch 2.13.0** (Homebrew py3.12 venv): both still crash at the op call (SIGBUS, no graceful error), so not fixed. One bounds-check fixes both. Both belong under crash umbrella #88148. |
| 2026-07-30 | #189667 → duplicate of #188959 (kokol16) | Closed #189667 as `DUPLICATE` via helper; ported `actionable` + `topic: fuzzer` labels to #188959; retitled #188959 to cover both fold/unfold | Adjacent im2col/col2im family: unfold=`im2col_kernel` (#189667, illegal write), fold=`col2im_batched_kernel` (#188959, illegal read). Same root cause — integer overflow in the im2col/col2im shape/index math under near-INT64 kernel/dilation/padding/stride, so one bounds/overflow guard fixes both. Kept the older #188959; new title: "[CUDA] illegal memory access on fold/unfold (col2im/im2col) with integer-overflow kernel/dilation/padding/stride". Part of kokol16's compute-sanitizer OOB batch; only the truly-adjacent operator pairs are being deduped (rest left as distinct). |
| 2026-07-30 | #191566 → duplicate of #189664 (kokol16) | Closed #191566 as `DUPLICATE` via helper; retitled #189664 to cover both 2d/3d | Max-pooling family: max_pool2d=`max_pool_forward_nchw` (#189664), max_pool3d=`max_pool3d_with_indices_single_out_frame` (#191566). Same root cause — 32-bit integer overflow on huge `dilation` (INT32_MAX) in the CUDA max-pooling kernels. Kept older #189664; new title: "[CUDA] illegal memory access on max_pool2d/max_pool3d with integer-overflow dilation". No richer labels to port (#191566 only carried the `triage review` workflow state). Part of kokol16's compute-sanitizer OOB batch. |
| 2026-08-07 | #192291 → duplicate of #192293 (ALinrunrun) | Closed #192291 as `DUPLICATE` via helper; labels already identical (no port needed) | **Alias of the same function.** `torch.i0` is a straight alias of `torch.special.i0` — both dispatch to the same ATen `i0` kernel. Byte-identical repro/expected in both (`x=713.0` float64 → `inf`, expected `6.705128263670996e+307`, premature overflow in the `exp(|x|)` path), filed by the same author 1m42s apart. Kept the *newer* #192293 (not the elder) because it carries the maintainer root-cause diagnosis + proposed fix in `SpecialMathKernel.cpp`, per the "canonical = whichever holds the resolution" rule. Sibling reports #192294 (`i1`), #192295 (`ndtr`), #192406/#192407/#192408 (`expm1`/`exp2`) are the same boundary-overflow *template* but distinct functions — umbrella candidates, left open, NOT deduped. |
| 2026-08-07 | #192294 → duplicate of #192293 (ALinrunrun) | Closed #192294 as `DUPLICATE` via helper; ported `module: edge cases` label to #192293; retitled #192293 to cover i0/i1 | **Confirmed shared code path.** In `aten/src/ATen/native/Math.h`, both `calc_i0` (large-\|x\| branch, line ~1484) and `calc_i1` (line ~1512) evaluate `std::exp(x) * chbevl(...) / std::sqrt(x)`; the bare `std::exp(x)` overflows to `inf` at \|x\| > ~709.78 *before* the `/sqrt(x)` scaling, so i0 and i1 overflow prematurely for the same inputs (~713.0) and one fix (log-domain / `i0e`·`i1e`-scaled evaluation) resolves both. (Note: I1 is the derivative of I0, not a product, but they share the same `exp(x)/√(2πx)` asymptotic and the identical un-scaled-`exp` bug.) Kept #192293; new title: "torch.special.i0/i1 (and torch.i0) overflow to inf at large finite float64 inputs (~713.0) although the result is finite". #192291 (torch.i0 alias) already folded here earlier today. Distinct siblings #192295 (`ndtr`), #192406/#192407/#192408 (`expm1`/`exp2`) left open (different functions/mechanism). |
| 2026-08-10 | #117210 (ghost/AspirantMelody) — closed as won't fix (no repro) | Closed on GitHub `not planned` with a comment | Old `module: mps` + `needs reproduction` report: "model.to('mps') gets stuck". Body is empty (just `/`), no version info; @malfet asked for an example in Jan 2024 and none was provided in 12+ months. Nothing actionable → closed as won't fix, inviting a fresh self-contained repro. DB re-synced (`state_reason=not_planned`). |
| 2026-08-10 | #111547 (ganesh1102) — closed as **fixed** | Ran repro path on torch 2.13.0 (Homebrew py3.13 venv, MPS); closed `completed` | Old `module: mps` + `needs reproduction`: internal assert `as_strided_tensorimpl does not work with MPS; call self.as_strided(...)` on torch 2.2.0.dev20231018, hit via `vmap(vmap(jacfwd(...)))` → `torch.cat` (forward-mode AD). MPS `as_strided` support landed ~2.2. Verified fixed on 2.13.0: both direct `as_strided` on MPS **and** the exact vmap/jacfwd/cat path run cleanly. DB re-synced. |
| 2026-08-10 | #126862 (chenlijn) — closed as won't fix (no repro) | Closed `not planned` with a comment | `module: mps` + `needs reproduction`: "worse results on MPS vs CPU" for the whole `lama` inpainting model. @jbschlosser asked which ops diverge / for a minimal repro; none was provided, no comments in >1 year. Closed as not actionable, inviting a fresh self-contained repro. DB re-synced. |
| 2026-08-10 | #132226 → duplicate of #89708 (xxxkkw) | Closed #132226 as `DUPLICATE` via `close_duplicate.py` | `module: mps` YOLOv5-on-MPS wrong-detection report (Chinese). @malfet had suspected it duplicates the M1 MPS thread #89708; user confirmed. #89708 is itself already closed, but is the canonical MPS-correctness thread to route this to. Not repro-tested (needs the user's trained model/video). |
| 2026-08-10 | #137001 (mbelotti-nuk) — closed as won't fix (no repro) | Closed `not planned` with a comment | `module: mps` + `needs reproduction`: BCE loss said to degenerate (predicts one class) on MPS vs CPU. @skotapati asked for a reproducing script; none provided, no comments in >1 year. Closed as not actionable. DB re-synced. |
| 2026-08-10 | #137964 (UjasShah) — closed as **fixed** | Ran the gist repro on torch 2.13.0 (Homebrew py3.13 venv), CPU vs MPS | `module: mps` + `needs reproduction`: WaveNet char-model "silently fails to train" on MPS, allegedly worse with the ReLU (script.py:73) removed. Ran the actual gist (script.py + names.txt from ~/Downloads). Both backends now converge identically — with ReLU: MPS ~2.09/2.37 vs CPU ~2.09/2.37; **without** ReLU (the skyrocket case): MPS ~2.21/2.43 vs CPU ~2.22/2.42. No silent failure → closed as fixed. DB re-synced. |
| 2026-08-10 | #151740 (ChenkaiMao97) — closed as **fixed** | Ran repro verbatim on torch 2.13.0 (Homebrew py3.13 venv), CPU vs MPS | `module: mps` + `needs reproduction`: FFT (`rfftn`/`irfftn`) + Adam training diverged on MPS (loss 0.99→4.36) while CPU converged, on torch 2.6.0. On 2.13.0 both backends are now identical — CPU and MPS both converge to `Loss: 0.991824` at step 1000. No divergence → closed as fixed. DB re-synced. |
| 2026-08-10 | #151667 (matkozak) — closed as **fixed** | Ran repro verbatim 5× on torch 2.13.0 (Homebrew py3.13 venv), CPU vs MPS | `module: mps` + `needs reproduction`: `nn.MultiheadAttention` with causal `attn_mask` + `key_padding_mask` + dropout produced `nan to nan` output on MPS while CPU was clean, on torch 2.6.0. On 2.13.0 the MPS output has no NaNs across 5 runs and matches CPU (range ~-4.30 to 4.42). Closed as fixed. DB re-synced. |
| 2026-08-10 | #169342 (thepowerthatbe) — closed as **fixed** | Stress-tested on torch 2.13.0, M5 Pro / macOS 26.5.2 (build 25F84) | `module: mps` + `module: convolution` + `module: correctness (silent)`: M5-specific — `chunk()` views through `Conv2d` gave wrong batch results (only batch elem 0 correct). Root cause was inside the MPS framework (per @jhavukainen), needing an OS-side fix; the PyTorch workaround PR #169935 (force-contiguous on M5) went **stale and was never merged**. On macOS 26.5.2 the repro passes, and a sweep of 108 configs (batch 2–16 × ch 16–128 × 32/64/128 × 3 seeds) all match (worst 2.4e-7). Earlier reports were 26.2/26.4 (tanqiukai still saw it on 26.4), so the OS fix landed in 26.5. Closed as fixed, noting users on macOS ≤26.4 should update. DB re-synced. |
| 2026-08-10 | #94765 (laclouis5) — closed as **fixed** | Ran thread's minimal repro on torch 2.13.0, M5 Pro / macOS 26.5.2 | `module: mps`: `torch.gather` with a singleton last dim hit an internal `MPSNDArrayGatherND … Rank of updates` assertion on the 2.0 nightly. On 2.13.0 the minimal repro (`gather` of `(1,40,1)` with `(1,20,1)` indices) runs cleanly and matches CPU. Confirms @aditvenk's non-repro (2026-01-30). Closed as fixed. DB re-synced. |
| 2026-08-10 | #83015 (alin256) — closed as **fixed** | Ran reporter's conversion loop + stress on torch 2.13.0, M5 Pro / macOS 26.5.2 | `module: mps`: `float64→float32` `.to('mps', non_blocking=True)` produced garbage (interleaved denormals/zeros) on torch 1.12 — root cause a **data race** in the non-blocking copy-with-cast (read before async cast finished; @kulinseth's adaptive-commit path). On 2.13.0 the converted values exactly match the CPU `float()` reference for both `non_blocking=True/False`; stress-tested 5 fresh runs × 200 iters, worst diff 0.0, no corruption. Race no longer observable → closed as fixed. Last substantive comment was 2023-10, nothing recent. DB re-synced. |
| 2026-08-11 | Intel-Mac / CUDA-on-Mac batch (8 issues) — closed as won't-fix (obsolete platform) | Closed `not planned` with a platform-support comment; user-approved list | Old `module: macos` issues obsolete by platform-support decisions, not per-issue repro. **CUDA on Mac** (macOS CUDA builds dropped years ago): #27409 (MAGMA rpath, OS X w/ GPU), #46803 (CUDA macOSX 10.13.6). **Intel/x86 macOS** (PyTorch no longer ships x86-64 macOS binaries; OS X 10.13 / pre-AVX EOL): #15291 (1.0 link error), #19672 (set_flush_denormal on old OSX), #33385 (Illegal instruction OSX 10.13.6 source build), #35414 (cmake --build failure), #39634 (source build fails macOS No-CUDA), #42784 (libtorch 1.5 torch::max without AVX). Excluded #44611 (complex tan/tanh correctness bug — later confirmed still reproduces on Apple Silicon, i.e. a macOS-libm issue, not x86-only; see `still_valid.md`) per user. Python-2 angle dropped: the ~99 "python 2" hits were mostly Caffe2 env-dump artifacts, not a clean close basis. DB re-synced (all `not_planned`). |
| 2026-08-11 | #116857 → duplicate of #98169 (MPS op sweep) | Closed #116857 as `DUPLICATE` via helper | Both report `aten::_weight_norm_interface` NotImplementedError on MPS. #98169 was already closed `completed` (op implemented; verified running on torch 2.13.0). #116857 is the same op/same request → deduped into #98169. |
| 2026-08-11 | #102852 (poisson) — closed as **implemented** | Verified on nightly `torch==2.14.0.dev20260810` (MPS) | `aten::poisson` NotImplementedError on MPS. Implemented on main but NOT in the 2.13.0 release (runtime-checked); confirmed `torch.poisson` runs on MPS on the nightly. Closed `completed`, noting it lands in the next release. Part of the MPS "not implemented" sweep (op names cross-checked vs `native_functions.yaml`, then runtime-verified). |
| 2026-08-11 | #136692 (linalg_matrix_exp) — closed as **implemented** | Verified on nightly `torch==2.14.0.dev20260810` (MPS) | `aten::linalg_matrix_exp` NotImplementedError on MPS. Implemented on main but NOT in 2.13.0 release; confirmed `torch.linalg.matrix_exp` runs on MPS on the nightly. Closed `completed`, noting next-release availability. |
| 2026-08-12 | Old macOS batch (5 issues) — closed as obsolete/done | Closed on GitHub with per-issue comments; user-approved | Non-MPS `module: macos` cleanup. **Conda no longer maintained** → #79337 (nightly conda channel), #73051 (conda into Docker on M1) closed `not planned`. **#56333** (can't pip install on M1) closed `completed` — arm64 pip wheels ship now. **#71704** ("M1 Pro chip support") closed `not planned` — no repro, just a harmless `NNPACK: Unsupported hardware` info message (NNPACK is x86-only, unused on Apple Silicon). **#79382** (torch can't open libcuda.so.1 on macOS) closed `not planned` — CUDA unsupported on macOS. DB re-synced. |
| 2026-08-12 | #61268, #33285 — closed as won't-fix (PyTorch Mobile → ExecuTorch) | Closed `not planned`, comment pointing to ExecuTorch | Deprecated on-device backends. #61268 = old PyTorch Mobile **Metal** backend (`metal.Conv2dOpContext`); #33285 = old iOS **libtorch** mobile runtime. Both superseded by ExecuTorch (Metal/MPS + CoreML). Not maintained → won't-fix with migration pointer. DB re-synced. |
| 2026-08-12 | Old macOS Category-A build batch (11 issues) — closed as won't-fix (obsolete/stale) | Closed `not planned` with per-issue comments; user-approved after deeper analysis | EOL/obsolete from-source-build issues. Intel/x86 & EOL OS: #21318 (x86 gtest linker), #31308 (conda+Py3.6), #51231 (1.7.1 on Catalina), #70394 (2009 Core 2 Duo, MKL SSSE3). Old release/conda packaging: #49405 (conda 1.7.1 libomp), #51084 (libtorch 1.7.1 bundle path). CUDA-on-Mac: #73062. Deprecated mobile: #60950 (CocoaPods LibTorch → ExecuTorch). Transient/stale: #79421 (June-2022 nightly clang 13.0/13.1 mismatch, resolved), #78831 (DEBUG=1, malfet couldn't repro, no follow-up), #74503 (stale early-M1 XNNPACK build). **Kept OPEN as still-valid: #51015** (protobuf `/usr/local/include` header clash — PyTorch still vendors `third_party/protobuf`) and **#23676** (bogus `cpp_extension` compiler-compat warning — warning path still present at `cpp_extension.py:336`). DB re-synced. |
| 2026-08-12 | #66775 (RahulBhalley) — closed as won't-fix (mobile Metal, not MPS) | Closed `not planned`, comment pointing to ExecuTorch / MPS | `module: macos`: "FFT operations on Metal" is a feature request for the old PyTorch Mobile **Metal** (on-device iOS) backend — deprecated/unmaintained, superseded by ExecuTorch. Not the desktop MPS backend (which already does `torch.fft` on `device='mps'`). Won't-fix. |
| 2026-08-12 | #6328, #7686 (implemented) + #63558 (won't-fix) — old macOS closes | Closed on GitHub with command-output comments | #6328 (libomp/OpenMP support) and #7686 (macOS pinned to one thread) both long since implemented — posted `torch.backends.openmp.is_available()=True` and `torch.get_num_threads()=6` on torch 2.13.0 into each; closed `completed`. #63558 (universal x86_64+arm libtorch) closed `not planned` — PyTorch ships arm64-only macOS binaries now, so a universal binary is not planned. DB re-synced. |
| 2026-08-16 | Blooming-Tree `torch.compile` "inconsistent outputs" batch (4 issues) — closed as won't-fix (**not** as duplicates) | Closed #188074, #188420, #188430, #188432 `not planned` with per-issue comments; DB re-synced | Serial-reporter batch sharing a byte-identical harness: **eager float64** (`model.double()`) vs **compiled float32** (`deepcopy(model).float()`), scored against a hand-picked `abs>0.01 \| rel>0.001` — tighter than float32 can deliver, and tighter than the default tolerances `pt2-bug-report.yml:23` asks for. Sibling #188425 was already closed `completed` by @eellison as expected behavior after @harsh-vsingh named #188420/#188430/#188432 as having the same methodology flaw. **Per maintainer: closed as won't-fix, not as duplicates of #188425.** Split into two comment types: (a) **RNG** — #188074 (`torch.bernoulli` in `forward`) and #188420 (`dropout_p=0.1` passed *functionally* to `F.scaled_dot_product_attention`, which `model.eval()` does **not** disable). (b) **Numerical** — #188430/#188432, where `eval()` does disable dropout, so the confound is purely dtype. Cited `notes/randomness.html`, `notes/numerical_accuracy.html`, and the `bug-report.yml:35` template line requiring "appropriate justification on why one result is wrong and the other is correct". |
| 2026-08-16 | Verified facts backing the batch above (keep for reuse) | Probes run on torch 2.13.0 (`cpuvenv`, CPU) | **(1) RNG streams are dtype-dependent**: same seed/device, `torch.bernoulli` on float64 vs float32 mismatches 517/1000 draws, diverging at draw 0; `torch.rand` gives 0.058154 vs 0.882269 (float64 consumes two 32-bit words per element). **(2) But dropout masks are NOT** — `F.dropout` and SDPA `dropout_p` produce *identical* masks across float32/float64, so the dtype-RNG argument applies to `bernoulli`/`rand` only; for SDPA the confound is RNG stream *position* (eager forward runs after weight-init + `get_sample_inputs()` draws; compiled forward runs right after a bare `manual_seed` + `deepcopy`). **(3) Decisive**: #188074's harness with `torch.compile` deleted, eager-fp64 vs eager-fp32, still flags 20/20 elements (abs max 9.27e-2, rel max 5.85) — the compiler is not involved. **(4) Amplification**: `torch.reciprocal` / `head_dim**0.5 / (q @ kᵀ)` on near-zero attention outputs magnifies ordinary fp32 rounding, which is why #188430 reports max abs diff 1.92 against eager values already in the ±2000 range. |
| 2026-08-18 | #77893 → duplicate of #128691 (cross-author: neochristou 2022 / LongZE666 2024) | Closed #77893 as `DUPLICATE` via `close_duplicate.py`; ported `module: vmap` to #128691 | **Confirmed identical missing check** (source read, not inferred). `aten/src/ATen/native/LegacyBatching.cpp` `_remove_batch_dim`: when the tensor carries no batch level it runs `expanded_sizes.insert(expanded_sizes.begin() + out_dim, batch_size)` with **no validation of `out_dim`** → out-of-range iterator → OOB write. Both reports pass the *identical* value `out_dim = 1250999896764`; input rank differs (5-D vs 10-D) and is irrelevant. One `TORCH_CHECK(out_dim >= 0 && out_dim <= self.dim())` fixes both. Both still hard-crash (SIGSEGV) on torch 2.13.0. Kept the **newer** #128691 per "canonical = whichever holds the resolution": it carries the ASAN `negative-size-param` trace naming the faulting frame, whereas #77893 was mislabeled `needs reproduction` despite having a complete repro. |
| 2026-08-18 | #77900, #141847 (slow_conv3d family) — closed as **fixed** | Closed both `completed` with the verbatim 2.13.0 error output | Ran both reproducers on torch 2.13.0 (cpuvenv, Python 3.12, macOS 26.6 arm64). Neither FPE reproduces; both now terminate gracefully. #141847 → `RuntimeError: Kernel height x width product is too large: kernel_height=36028797018963968, ...`; #77900 → `RuntimeError: ... SymIntArrayRef expected to contain only concrete integers` (negative args rejected at the dispatcher), and with a well-formed 5-D input + positive huge kernel the real domain check fires: `Kernel size can't be greater than actual input size`. Huge `padding` is guarded too (`Storage size calculation overflowed`). **Not deduped** — they were candidates for a same-op merge, but the question is moot once both are fixed. |
| 2026-08-18 | #73190 (max_pool1d, neochristou) — closed as **fixed**, with a caveat recorded in-thread | Closed `completed`; comment documents the overflow artifact and links the two live siblings | Repro no longer segfaults on 2.13.0; raises `RuntimeError: pad should be at most half of effective kernel size, but got pad=0, kernel_size=1250999896764 and dilation=1250999896764`. **But the guard fires only by accident**: `Pool.h`'s `TORCH_CHECK(pad <= ((kernelSize - 1) * dilation + 1) / 2)` overflows int64 for these values, making the bound **-1289383578495887446**, so `pad = 0 <= bound` fails — hence the nonsensical "got pad=0" message. Had the overflow landed the other way the computed output size would have been a bogus positive and the kernel would have read OOB. **NOT deduped with its same-op siblings** — they have different roots and stay open (see next row). Closed at maintainer's request. |
| 2026-08-18 | max_pool1d / native_batch_norm — **rejected** as dedup candidates (analysis, no close) | No action taken; recorded so a later pass doesn't re-propose these merges | Cross-author same-op pairs that are **not** the same boundary check. **max_pool1d**: #142454 = int64 overflow of `dilation*(kernelSize-1)` in `pooling_output_shape` → bogus `OW=1` passes `TORCH_CHECK(OW > 0)` → SIGSEGV (same root expression as the now-closed #73190). #136719 = **different root**: `aten/src/ATen/div_rtn.h` declares `int q`/`int r` instead of `T`, so for int64 the true remainder −9223372036854775803 truncates to **+5**, the sign test skips the floor correction, and `OW` becomes **1 instead of 0** — the existing guard would have caught it. Blast radius: every call site passes `div_rtn<int64_t>` (`Pool.h`, `im2col_shape_check.h`, `ConvolutionMM2d/3d.cpp`, `DilatedConvolutionUtils.h`, CUDA `Im2Col.cu`/`Col2Im.cu`, MPS `Pooling.mm`) — including the im2col/col2im check behind #188959. Worth its own issue. **native_batch_norm**: `batch_norm_cpu` validates nothing at entry; #141203 = `train=false` with **both** running stats `None` → undefined-tensor deref (the asymmetric case *is* checked: "running_mean and running_var must either both be None or neither be None"); #85217 = `train=true` with running stats of numel 0 while C=5 → OOB **write** during the in-place update. Same unvalidated function, two different holes → both left open. Side finding: a wrong-size `weight` (numel 3 vs C=5) is accepted **silently** (no error, no crash) — a likely silent OOB read, per the sanitizer policy "completes silently" is inconclusive, not safe. |
| 2026-08-18 | **Correction: #73190 close reverted** — reopened by maintainer, still reproduces | Issue reopened on GitHub (`state_reason=reopened`); DB re-synced. Kept-open details now in `still_valid.md` | My close-as-fixed above was wrong: it generalized from the reporter's *one* constant. `kernel_size = stride = dilation = 1250999896764` is caught only because `(kernelSize-1)*dilation` overflows int64 and drives the pad bound negative. With **`i = 2147483697`** (just above `INT32_MAX`) the product `4611686226733304112` does **not** overflow, the pad check passes legitimately, and the repro still **SIGSEGVs on torch 2.13.0**. Root cause is the `div_rtn` int32 truncation, not the overflow path: correct floor quotient `-2147483695` → `OW = -2147483694` would be rejected by `TORCH_CHECK(OW > 0)`, but `int q` wraps it to `+2147483602` so the guard passes. **Lesson for this repo: for out-of-domain-integer crash reports, do not close as fixed on the reporter's single constant — sweep neighbouring magnitudes (especially around `INT32_MAX`) before concluding a bound exists.** |
| 2026-08-28 | #104832 (chrisdesa) — closed as **fixed** by maintainer; independently verified here | Ran the repro on torch 2.13.0 (`cpuvenv`, Python 3.12), M4 Pro / macOS 26.6.2; DB re-synced with `fetch_issue.py --refresh 104832` | `module: mps` + `module: correctness (silent)`: `torch.matmul(X, Y, out=Z[:, 128:384])` (non-contiguous `out=` view) returned zeros and left `Z` unchanged on 2.0.1 / macOS 13.4 / M2 Max, with suspected memory corruption. **Confirmed fixed:** the strided `out=` is written correctly, the returned tensor matches `X@Y`, and nothing outside the target slice is touched; clean across n ∈ {64,128,256,512,1024}. Matches the issue's own history — @hvaara reproduced on main in 2024-09, then could not after a macOS upgrade in 2024-12, concluding it was an OS-side fix. Closed by @malfet 2026-08-28 ("fixed a while back, not reproducible on sufficiently recent PyTorch/MacOS"). **Verification note:** CPU and MPS fp32 matmul come out **bitwise identical** here, which looks like a stub until checked — both carry the same 2.3e-04 error against a float64 reference, and the GPU is genuinely working (5× 4096³ in 0.121 s ≈ 4.6 TFLOPS). |
| 2026-08-28 | #117826 (chenmingxiang110) — closed as **fixed** | Tested the gradient claim directly on torch 2.13.0 **and** 2.8.0 (`cpuvenv` / `.venv`), M4 Pro / macOS 26.6.2; closed `completed` with the numbers; DB re-synced | `module: mps`: reported `grad(naive_cpu)==grad(naive_mps)==grad(einsum_cpu)!=grad(einsum_mps)` for `torch.einsum("bij,ijk->bik", xs, weight)`. Original repro is a training-loop script, so tested the claim itself: forward + `d_xs` + `d_weight` for einsum vs a naive per-head `xs[:,i] @ weight[i]` reference, across cpu/mps. All four combinations agree to **4.768e-07**; decisively, `einsum` on **CPU** shows the *same* 4.77e-07 offset from naive-CPU, so the residual is einsum-vs-naive reassociation, not MPS. At `[B=256, heads=8, 64→64]` einsum-mps vs einsum-cpu differ by **exactly 0**. **No fixing PR identifiable:** `git log --grep` finds no commit referencing #117826, and it already passes on **torch 2.8.0** on this machine, so the fix predates 2.8.0 — likely macOS-side, as with #104832 (whose thread attributes the fix to an OS upgrade). Last comment 2024-01-25. |
| 2026-08-28 | Fixing-PR search for the old MPS silent-correctness batch — **mostly unattributable** (analysis, recorded to avoid redoing it) | `git log --all --grep` per issue number over `~/git/pytorch/pytorch`; `git tag --contains` on candidate commits | **No commit references #117826, #96982, #104832, #96225 or #142836.** For #104832 the closest PyTorch work is @Isalia20's **#187255** ("Matmul for strided out errors on mac OS 14/15", commit `215923c3574`) and **#193385** ("Fix mm/addmm/bmm producing garbage when `out=` is a tensor slice on macOS 14", commit `6372ada3d50`) — but `git tag --contains` puts #187255 only in `v2.14.0-rc*` and #193385 in **no release tag at all**, while the repro already passes on 2.13.0 here. So on macOS 26 the underlying MPSGraph defect is gone **OS-side**, and those PRs are workarounds for users still on macOS 14/15. Same pattern for #96982 and #117826: both already correct on torch **2.8.0** on macOS 26. **Takeaway for this repo: for old MPS correctness bugs, "fixed" often means Apple fixed MPSGraph, not that a PyTorch PR landed — don't go hunting for a fixing PR before checking whether an older torch also passes on the current OS.** |
| 2026-08-31 | ☂️ **#195547 created** — umbrella for "missing shape checks → out-of-bounds tensor access"; **#194799, #194800, #194802, #194803, #194805, #194807 → duplicate of #195547** (re-closed) | Created #195547 (no labels/cc — left to the triage bot). The six were already `closed`/`completed` from the 2026-08-31 maintainer pass, so each was **reopened and re-closed via `close_duplicate.py`** to get the real `DUPLICATE` state_reason + "marked as duplicate of #195547" link. Removed `malfet` as assignee from #194800/#194802/#194805. Twelve still-open members cross-linked by comment (**track** mode, not consolidate — kept open as the record of each reproducer): #194048, #194049, #194798, #194801, #194804, #194806, #128692, #128695, #145259, #153967, #162358, #170573. DB re-synced for all 18 hanbaiGG issues. | Serial-reporter batch (`hanbaiGG`, 18 ASAN-fuzzer reports in two waves: #194047–194054 on 08-19, #194798–194807 on 08-25) with one shared root: an op derives its iteration geometry from one argument, then dereferences a *different*, unvalidated argument (output buffer, running stat, `padding`/`output_size` list) that is too small. Exactly the `module: error checking` class in CLAUDE.md. @drisspg had left "Make umbrella issue for all this bwd in isolation error handiling" on all six closed ones, so the umbrella was the explicitly requested disposition; no existing umbrella matched (searched ☂️/umbrella/tracking). Umbrella body states the maintainer position: **per-op `TORCH_CHECK` PRs are not wanted** — the ask is a device-agnostic check ahead of dispatch, derived from the schema/meta function, i.e. continued structured-kernel migration. **Excluded on purpose:** #194052/#194053 (inductor `_reinterpret_tensor`/`_alloc_from_pool` — jansel: offsets belong to the compile-time memory planner, different fix); #194047 (failed `resize_` leaves inconsistent metadata, not a missing shape check); #194050/#194051/#194054 (already closed by maintainers on distinct reasoning: private ops, dup, internal tracing shim); #152777/#154419/#155393 (root not characterized well enough) and #165987 (memory *aliasing*, not shape validation). Severity note carried into the umbrella: most repros need direct boxed dispatch of a private/backward op and are only visible under ASAN, so this is hardening, not a user-facing crash. |
| 2026-09-02 | **45 issues → duplicate of ☂️ #195547** (LongZE666 ×21, shijy16 ×15, WLFJ ×9) | Closed each via `close_duplicate.py` (real `DUPLICATE` state_reason + "marked as duplicate of #195547" link) with a comment pointing at the umbrella and noting that per-op `TORCH_CHECK` PRs are not the fix being sought. DB re-synced per issue. Verified afterwards in one GraphQL batch: **45/45 `CLOSED/DUPLICATE`**. Members: #128691, #128693, #128694, #140945, #141203, #141449, #141450, #141481, #142209, #142454, #142459, #142837, #142838, #143480, #143481, #143482, #143483, #143484, #143485, #143487, #143490 (LongZE666); #88036, #94590, #94593, #94594, #94668, #94669, #94696, #94698, #94808, #96275, #96276, #96277, #97087, #97088, #98136 (shijy16); #145064, #145071, #145267, #145604, #146788, #146789, #146790, #146793, #146794 (WLFJ). | Candidate groups 1–3 from the sweep recorded in `malfet/pytorch_issue_crawler#1`, approved by 👍 on the corresponding comments. All are the umbrella's root cause: an op derives geometry from one argument then dereferences a *different*, unvalidated argument that is too small. **#147077 deliberately held back** from the approved WLFJ group — its root is that `torch.compile` *loses* the `RuntimeError` eager raises, i.e. a check that exists is dropped, not a check that is missing. **Chain note:** #128691 was itself the *kept* canonical of the 2026-08-18 dedup (#77893 → #128691), so that pair now chains into the umbrella. **Prior-analysis note:** #141203 and #142454 were rejected on 2026-08-18 as *pairwise* dedup candidates against other same-op reports; that rejection concerned 1:1 merges on a specific boundary check and does not conflict with folding them into the class umbrella. **Age caveat, flagged and accepted:** the shijy16 (2022-10 → 2023-04) and older LongZE666 reports were classified from report text, not re-run against current torch — per the CLAUDE.md repro rule some may already be fixed. Closing as a *duplicate* asserts shared root cause, not that the bug is live, so the umbrella remains the place where liveness gets settled. **Tooling note:** a single `for` loop over all 45 closes failed silently (0/45, no comments posted, no state changed) when run as a compound command with output suppressed; re-running in batches of ~10 with `stderr` visible worked. Verify state with one batched GraphQL query rather than 45 `gh issue view` calls — the latter returns empty under burst. |
| 2026-09-13 | **39 CUDA issues → duplicate of ☂️ #195547** (kokol16 ×34, jwnhy ×5) | Closed each via `close_duplicate.py` (real `DUPLICATE` state_reason) with a comment noting the CUDA framing; DB re-synced per issue; verified in one batched GraphQL query: **39/39 `CLOSED/DUPLICATE`**. Checkboxes ticked on `malfet/pytorch_issue_crawler#1`. Members: #144611, #144612, #145349, #145350, #145453 (jwnhy, 2025-01); #188958, #188959, #189664, #189666, #189668, #189669, #189670, #189671, #189829, #189831, #191565, #191567, #191568, #191570, #191848, #191850, #192452, #192453, #192454, #193095, #193096, #193097, #193099, #193102, #193104, #193689, #193691, #193694, #193696, #193698, #195185, #195186, #195187, #195188 (kokol16, 2026-07 → 2026-08). | `[CUDA] illegal memory read/write on <kernel>` reports, surfaced by **compute-sanitizer** rather than ASAN. Maintainer call: these are the *same* class as the CPU reports already under #195547 — a missing device-agnostic bounds/shape contract — so **no separate CUDA umbrella**; sanitizer reports driven by **integer-overflowing `dilation`/`padding`/size arguments** are explicitly in scope for this umbrella too. Severity caveat retained from CLAUDE.md: where a compute-sanitizer trace is the *only* evidence (no Python exception, no user-visible crash), this is hardening, not a user-facing crash. Approved by 👍 on the group comment in `malfet/pytorch_issue_crawler#1`. |
| 2026-09-13 | #196906 (tangyuan499-commits) → **duplicate of #146910** (cybersupersoap) | Closed #196906 via `close_duplicate.py` (real `DUPLICATE` state_reason); ported `module: error checking` onto #146910 (verified the label exists — note there is also a distinct `module: shape checking`); DB re-synced for both; verified #196906 `CLOSED/DUPLICATE`, #146910 still open. | **Identical assert, verified in source rather than inferred.** Both reports hit `TORCH_INTERNAL_ASSERT(src.layout() == kSparseCsr \|\| src.layout() == kSparseCsc)` in `add_out_dense_sparse_compressed_cpu`, `aten/src/ATen/native/sparse/SparseCsrTensorMath.cpp:826-828` (reported as `:817`; line drift since 2025). They approach from opposite directions — #146910 is `m.add_(bias)` with a CSR `m` and a dense zero-size `bias`; #196906 is `torch.add(dense, dense, out=csr)` — but dispatch reaches the same function with a strided `src` because the layout combination is never validated up front, so one layout guard fixes both. Meets the CLAUDE.md "identical error string / traceback location" bar. **Not** folded into ☂️ #195547: this is the `TORCH_INTERNAL_ASSERT`-on-invalid-input class explicitly excluded from that umbrella — a check *does* fire, it is just an internal assert where a user-facing "unsupported layout combination" error belongs — which is precisely what the reporter asked for. **Red herring rejected:** #131318 sits in the same file and also reports an internal assert, but it is a *different* one (`input_dim == 2` at `:1306`, from summing a 3-D CSR tensor) — not a merge candidate. Kept the older #146910 as canonical under the default age rule; neither issue holds a fix or richer diagnosis. |
| 2026-09-13 | **6 issues → duplicate of ☂️ #195547** (x0w3n group, 2024-05/09) | Closed #125664, #136704, #136709, #136712, #136714, #136819 via `close_duplicate.py`; DB re-synced; checkboxes ticked on `malfet/pytorch_issue_crawler#1` (group now 6/7). | Approved by 👍 on the x0w3n group comment. Degenerate inputs to FFT, weight-norm, quantized RNN cells and sparse ops reaching a kernel with no size/shape `TORCH_CHECK` — the umbrella's root cause. **#136719 deliberately held back** from the approved group, see next row. |
| 2026-09-13 | #136719 (x0w3n, `max_pool1d`) — **held back** from the umbrella fold-in (no action taken) | None; recorded so a later pass doesn't fold it in by default | Not a *missing* check but a **wrong** one, and it already has a named root cause: `aten/src/ATen/div_rtn.h` declares `int q`/`int r` instead of `T`, so for int64 the floor quotient wraps positive, the output-size guard accepts a shape it should reject, and the kernel then reads OOB (full analysis in `still_valid.md`, rows 2026-08-18/19). Folding it into a 90-member umbrella would bury a specific, already-diagnosed defect. Its fix PRs — [#194112](https://github.com/pytorch/pytorch/pull/194112) (`div_rtn` + `pooling_output_shape`) and [#195035](https://github.com/pytorch/pytorch/pull/195035) (`at::divup`) — are both **CLOSED UNMERGED** as of 2026-08-31/28, so the bug is live and unowned; it wants a revived PR, not a dedup. Same reasoning as #147077 (WLFJ group), also held. **Inconsistency to be aware of:** its sibling #142454 — same pooling-overflow family, different overflow (int64 wrap in `dilation*(kernelSize-1)`, also covered by #194112) — *was* folded in with the approved LongZE666 group. If #136719 goes in, #142454's diagnosis is already inside the umbrella; if the pooling-overflow work is revived, both should come back out together. |
