# Deduplication log

Audit trail of duplicate-issue decisions. Newest entries at the bottom. See
`CLAUDE.md` for the recording policy and `dedupe_candidates.py` for how
candidates are generated.

| Date (UTC) | Decision | Action | Reasoning |
|------------|----------|--------|-----------|
| 2026-07-15 | #127009 → duplicate of #126947 | Closed on GitHub (`not planned`), commented "Duplicate of #126947" | Same author (`hyperkai`), filed the same day (~18h apart). Both report `dsplit()` with `indices_or_sections=` not working while the positional form works — #127009 is a reworded restatement of #126947. Cosine similarity 0.89; shared terms: `indices_or_sections`, `dsplit`, `torch.dsplit`, `sections`. |
| 2026-07-16 | PR #180891 → duplicate of PR #190014 (kept #190014) | Closed #180891 on GitHub, commented pointing to #190014 | Duplicate-effort PRs (different authors) for the same `rocm_smi`→`amd_smi` migration; #190014 is an explicit rebased sequel of #180891. Both CLA `SUCCESS`. Older #180891's **last commit** was 2026-04-20 (stale ~3 months) vs. #190014's 2026-07-15 and `MERGEABLE` — so under the PR rule the older/stale one is closed in favor of the newer active one. Note: #180891's `updatedAt` (2026-07-13) was misleading cc/bot comment noise; staleness was judged by last commit date. |
| 2026-07-17 | #107543 and #107556 (dmc1778) — closed **both**, not one-as-dup | Added `module: nn` label to both; closed both `not planned` with an "expected behavior" comment | Same case (surfaced as a near-duplicate pair, cosine 0.73): both call `AdaptiveAvgPool2d(~1.25e12)`, requesting a ~1.25e12² output whose `numel` overflows int64 (`numel: integer multiplication overflow`). Not an operator bug — nonsensical output size can't be allocated. Per maintainer decision, closed both as expected behavior rather than deduping one into the other. |
| 2026-07-17 | #91557 → duplicate of #91556 (dmc1778) | Closed #91557 `not planned` via `close_duplicate.py`; added `module: nn` (+ existing `module: crash`) | Identical reproducer to #91556 (`AdaptiveMaxPool3d([125091515651]*3)`). #91557 segfaulted on torch 1.12/1.13; the crash was later fixed by adding a checked error (`Storage size calculation overflowed`), which is what #91556 already shows — so same case, closed as duplicate. |
| 2026-07-17 | #91556 (dmc1778) — closed as expected behavior | Added `module: nn`; closed `not planned` with "expected behavior" comment | Requests an `AdaptiveMaxPool3d` output of 125091515651³ elements; storage size overflows int64 and is correctly guarded (`Storage size calculation overflowed`). Nonsensical output size, not a bug. #91557 (the segfault variant) is a duplicate of this. |
| 2026-07-17 | Correction: re-mark #91557 and #127009 with real `DUPLICATE` reason | GraphQL `closeIssue(stateReason:DUPLICATE, duplicateIssueId:…)` | They had been closed as `not_planned` (all `gh issue close --reason` exposes), which doesn't show the "marked as duplicate of #X" relationship. Re-closed via GraphQL so `state_reason=duplicate`. `close_duplicate.py` now does this automatically for issues. PRs (e.g. #180891) have no duplicate state_reason, so they stay closed-with-comment. Expected-behavior closes (#91556/#107543/#107556) correctly remain `not_planned`. |
| 2026-07-17 | #136568 → duplicate of #136452 (hyperkai) | Closed #136568 as `DUPLICATE` via `close_duplicate.py`; added `module: complex` to #136452 | Both report `linalg` norms returning a real `float` tensor for complex input while ignoring `dtype=torch.complex64` (torch 2.3.0). `linalg.norm` on a 1-D input dispatches to `linalg.vector_norm`, so #136452's vector case *is* #136568 — kept the older/general #136452. `matrix_norm` counterpart #136621 left open (distinct 2-D function). |
| 2026-07-18 | #136452 — repro test, **kept open** | Ran the reproducer on torch 2.8.0 (throwaway venv) | Still reproduces: `linalg.norm(complex, dtype=torch.complex64)` returns `tensor(8.6023)` as `float32`, silently ignoring `dtype` — identical to the 2.3.0 report. Not closing as fixed. (Nuance for maintainer: a complex vector's norm is real, so float output is arguably WAI; the only gripe is `dtype` being ignored rather than erroring.) |
